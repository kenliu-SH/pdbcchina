export function splitArrayIntoChunks(array: any, chunkSize: number = 25) {
  const result = [];
  for (let i = 0; i < array.length; i += chunkSize) {
    result.push(array.slice(i, i + chunkSize));
  }
  return result;
}
